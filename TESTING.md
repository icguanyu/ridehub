# 測試

## 自動化煙霧測試（E2E）

涵蓋司機註冊/登入、服務設定、公開頁、客人預約（含價格、營運時間、當日額滿、token）、
司機接受/拒絕、統計，共 34 項斷言。會自動建立並清除測試資料。

```bash
cd backend
npm run dev          # 另一個終端機先啟動後端
npm run smoke        # 執行測試（讀 backend/.env）
```

全綠才算通過；退出碼非 0 代表有失敗。

## 手動測試清單（上線前）

### 司機流程
- [ ] 註冊 → 自動導向服務資訊頁
- [ ] 登入 / 登出 / 重新整理後仍保持登入
- [ ] 編輯服務資訊、定價 → 儲存後重載仍在
- [ ] 設定營運時間、每日名額
- [ ] 綁定 LINE User ID
- [ ] 複製專屬連結 / QR Code 可掃描
- [ ] 總覽顯示統計與待確認預約
- [ ] 接受預約 → 統計即時更新、該筆離開待確認
- [ ] 拒絕預約（含原因）
- [ ] 預約列表分頁與狀態篩選

### 客人流程
- [ ] 開啟 `/driver/:id` 看到司機公開資訊（不含私人電話）
- [ ] 預約表單必填驗證、手機格式驗證
- [ ] 距離改變時預估價格即時更新
- [ ] 送出後導向狀態頁，顯示「待確認」
- [ ] 司機接受後，狀態頁「重新整理」顯示「已接受」+ 司機電話
- [ ] 錯誤 / 缺少 token 時狀態頁顯示提示，不外洩資料

### 通知（需設定 LINE 金鑰）
- [ ] 建立預約 → 司機 LINE 收到推播
- [ ] 客人同意 / 不同意報價 → 司機 LINE 收到推播
- [ ] 未設定 LINE 金鑰時記為 `skipped`；`notifications_log` 有對應紀錄

### 非功能
- [ ] 手機版面（Chrome DevTools 375px）排版正常
- [ ] 404 頁面
- [ ] Supabase RLS：用 anon key 直接讀 `drivers` 應讀不到他人資料
- [ ] `.env` 未進 git（`git check-ignore backend/.env frontend/.env`）
- [ ] 前後端 `npm run build` 均成功
